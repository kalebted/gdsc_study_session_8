import 'package:flutter/material.dart';

class BookStore2 extends StatelessWidget {
  const BookStore2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        
    );
  }
}